{
  "behaviorID": "ExploringDispatcher",
  "behaviorClass": "DispatcherStrictPriority",

  "maxBehaviorActivations": 1,

  "interruptActiveBehavior": false,

  "behaviors": [
    // ExploringVoiceCommand wants to run only if voice command is pending.
    // it must be first to clear an intent if it exists
    "ExploringVoiceCommand", 
    "Exploring"
  ]
}
