{
  // NOTE: this file is just here to avoid breaking the executableBehaviorType checks currently in place. When
  // we want to properly expose these to SDK, we should reconsider how this works (and re-organize where these
  // behaviors live)

  "behaviorClass": "FindFaces",
  "behaviorID": "FindFaces_sdk",
  "executableBehaviorType": "FindFaces",
  
  "maxFaceAgeToLook_ms": 180000,

  "stoppingCondition": "None",
  "searchBehavior": "LookInPlaceHeadUp"
}
