{
  "activityID"   : "BuildPyramid",
  "activityType" : "BuildPyramid",
  "activityStrategy":
  {
    "type" : "Pyramid",
    // at least interact for this amount of time every time
    "activityCanEndDurationSecs" : 5.0,

    // stop running after this time (want to stop, don't actually stop until behavior is done)
    // NOTE: this should never really be hit, only if a bunch of new behaviors get added
    "activityShouldEndDurationSecs": 300.0,
    "cooldownSecs" : 300.0
  },
  "setupChooser" :
  {
    "type" : "scoring",
    "behaviors" : [
      { "behaviorID" : "DriveOffCharger",
        "scoring"      :
        {
          "flatScore": 1000
        }
      },
      { "behaviorID" : "KnockOverCubes",
        "scoring"      :
        {
          "flatScore": 20
        }
      },
      { "behaviorID" : "PyramidPutDownBlock",
        "scoring"      :
        {
          "flatScore": 5.0
        }
      },
      { "behaviorID" : "SparksLookInPlace",
        "scoring"      :
        {
          "flatScore": 2.0
        }
      }
    ]
  },
  "buildChooser" :
  {
    "type" : "scoring",
    "behaviors" : [
      { "behaviorID" : "DriveOffCharger",
        "scoring"      :
        {
          "flatScore": 1000
        }
      },
      { "behaviorID" : "KnockOverCubes",
        "scoring"      :
        {
          "flatScore": 20
        }
      },
      { "behaviorID" : "RespondToPyramidBase",
        "scoring"      :
        {
          "flatScore": 5.0
        }
      },
      { "behaviorID" : "SparksPickupSingleCubeForPyramid",
        "scoring"      :
        {
          "flatScore": 4.0
        }
      },
      { "behaviorID" : "SparksLookInPlace",
        "scoring"      :
        {
          "flatScore": 2.0
        }
      }
    ]
  }
}
