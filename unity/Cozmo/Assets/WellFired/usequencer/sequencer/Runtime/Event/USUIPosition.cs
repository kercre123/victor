namespace WellFired
{
	/// <summary>
	/// Position at which to display a UI element.
	/// </summary>
	public enum UIPosition
	{
		Center,			// In the Center
		TopLeft,		// At the top left
		TopRight,		// At the top right
		BottomLeft,		// At the bottom left
		BottomRight,	// At the bottom right
	};
}