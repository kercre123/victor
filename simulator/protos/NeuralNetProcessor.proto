PROTO NeuralNetProcessor [
  
  field SFString name "NeuralNetProcessor"  

  # Generally slower than that in vision_config.json, to simulate robot processing time
  field SFInt32 pollingPeriod_ms 250  

]
{
Supervisor {
  name IS name
  controller "webotsCtrlNeuralNets"

  controllerArgs "resources/config/engine/vision_config.json resources/config/engine/vision/dnn_models resources/../../../playbackLogs/webotsCtrlGameEngine2/vision/object_detector/"

  # Add a bogus Lua block to get Webots to quit complaining there's no `IS` field for
  # for the pollingPeriod_ms parameter :-/
  %{ dummy="" }%
  
} # Supervisor definition
} # PROTO definition