buildscript { 
    repositories { 
        mavenCentral();
    }

    dependencies { 
        classpath 'com.android.tools.build:gradle:0.12.2'
    }
}

task clean(type:Delete ) {
    delete "build/"
    delete ".gradle/"
}
